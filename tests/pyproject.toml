[project]
name = "esque-compatibility-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.0",
    "testcontainers[elasticsearch]>=4.8.0",
    "httpx>=0.27.0",
    "pyyaml>=6.0.0",
]
